# 术语表（示例）

> 示例映射用于说明格式，实际内容需从代码、配置与存储映射验证。代码标识采用中性示意写法，不代表项目应使用某种命名风格。

## 业务术语映射

| 中文术语 | 英文名称/缩写 | 代码实体 | 数据表 | 置信度 |
|----------|---------------|----------|--------|--------|
| 业务记录 | Business Record | `business_record`（示例） | `business_records`（示例） | 示例 |
| 记录明细 | Record Item | `record_item`（示例） | `record_items`（示例） | 示例 |
| 请求方 | Requester | `requester`（示例） | `requesters`（示例） | 示例 |

## 使用规则

- 用户用词与本表冲突时，先请求确认，再更新术语表。
- 定位代码时，优先使用已验证的英文名称、缩写、实体名和表名。
- 无法从代码验证的映射标记为“低置信度”或“需用户提供”。