# 业务分析与决策

## 1. 业务流与数据流
分离记录，双向关联。业务流描述"做什么"，数据流描述"数据怎么变"。通过 data-flow.md 全局映射表关联。

## 2. 变更影响分析

| 场景 | 动作 |
|------|------|
| 新增/修改业务步骤 | 定位步骤 → 扫描关联数据流节点 → 评估 DB/API/测试影响 |
| 新增/修改数据流节点 | 定位节点 → 反向查业务步骤 → 确认用户可见影响 |
| 数据库表结构变更 | 找依赖节点 → 评估读写影响 → 确定迁移需求 |
| 外部 API 变更 | 契约对比 → 评估解析/降级 → 版本兼容 |

输出影响清单：受影响元素、类型、影响说明、优先级。

## 3. 决策框架

四步流程：识别冲突 → 方案对比表（≥2 方案，各 ≥2 优缺点）→ 推荐方案及理由 → 标注影响范围。

决策记录在 `decision-log.md`，包含：编号、日期、标题、冲突点、背景、方案对比、最终选择、影响范围、跟进。

新建决策条件（三者同时满足）：难以撤销、无上下文会惊讶、存在真实权衡。任一不满足则不建档。

### 决策优先级

1. 数据安全/正确性 > 性能
2. 可维护性 > 代码简洁性
3. 显式 > 隐式
4. 内部一致性 > 外部最佳实践

## 4. 回查

每次变更前：
1. 检索 `decision-log.md` 索引表，关键词匹配。
2. 命中冲突 → 输出冲突提示（编号+摘要+冲突点+调和建议）并暂停。
3. 不冲突则标注关系（补充/增强/替代），继续。

## 5. 破坏性影响分析（业务维度）

检查：规则冲突、目标冲突、流程冲突、语义冲突、权限冲突。

> 完整破坏性分析（分级、预测、标记、累积升级）以 `destructive-analysis.md` 为准。
