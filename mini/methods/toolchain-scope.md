# 工具链作用域

> **进入**：生成器、构建/格式化/编译工具、CI/CD 或生成物变化。仅运行既有命令且不改变配置、作用域或产物契约时跳过。

- 只修改获授权且实际生效的最小层级：项目本地优先于用户级，用户级优先于全局；在某目录执行命令不等于授权修改更高层级。
- 同时检查声明/锁文件、配置、生成物、CI/CD、容器和部署环境。
- 生成或迁移动作应可重复、可审查，并记录必要命令与版本。
- 验证实际产物及目标环境是否采用；命令成功不证明产物完整。

所有受影响层级和产物均验证后退出。
