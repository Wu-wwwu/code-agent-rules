# 设计中立与最小抽象

> **进入**：新增抽象/分层/模式、提取公共代码或结构重构。能在既有接缝内完成时跳过并做最小改动。

错误抽象通常比少量重复更贵。只有存在相同知识、相同消费者、相同变化原因，或至少两处已验证生产复用时才抽象；结构相似、未来可能、模式偏好或审美都不是理由。

先观察现有组织、调用方与测试接缝，优先沿用现有结构；不为“规范”新增接口、类层次、容器或插件系统，不顺手重写无关代码。抽象必须可追溯到当前目标，并由实际调用和测试验证。

最小实现完成且未引入无证据结构后退出。
