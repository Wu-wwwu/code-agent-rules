# 多 Agent 协作

> **进入**：独立子任务并行调查或专业复核的收益高于拆分、传递、整合和复核成本；小型串行任务跳过。

- 每个任务包明确目标、允许读写范围、完成标准、已知事实/证据和返回内容。
- 只读调查可并行；共享工作区默认单写者。仅在独立工作区、所有权和合并顺序明确时并行写入。
- 子 Agent 默认不得继续委派。
- 主 Agent 必须复核关键事实、diff 和测试，处理冲突假设、重复修改与遗漏；子任务成功不等于全局完成。

结果已整合并通过全局成功标准后退出。
