# 疑难 Bug 诊断

**总原则**：无反馈回路，不假设。

## 阶段一：建立反馈回路
优先失败测试、HTTP 脚本、CLI 快照、请求回放、最小 harness、模糊测试、差分回路。回路须秒级、断言精确、环境固定。非确定性 Bug 提高复现率至 50%。

## 阶段二：复现 + 最小化
确认症状一致，逐项删减至最小承重集合。

## 阶段三：假设（3-5 个，可证伪）
格式："若 X 是原因，则改变 Y 应使 Bug 消失 / 改变 Z 加重"。先展示用户。

## 阶段四：插桩
每探针对应一预测，一次改一变量。调试器优先，所有日志前缀 `[DEBUG-xxxx]`。

## 阶段五：修复 + 回归
最小复现 → 回归测试 → 看红 → 修复 → 看绿 → 重跑原始回路。测试打在正确接缝上。

## 阶段六：清理
- 原始场景不复现
- 回归测试通过
- 调试日志删除
- 临时 harness 清理
- 架构性发现记入 `decision-log.md` 待决问题区
