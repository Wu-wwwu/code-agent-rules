# 数据流推导

## 六步追踪法
1. 确定入口 2. 追踪传递 3. 记录变换 4. 标记存储 5. 确认出口 6. 绘制链路

## 固定推导顺序
入口 → 查询 → 变换 → 调用 → 解析 → 写入 → 出口（按实际存在环节，不强行填充）

## 节点标注
节点名称（动词短语）、类型（DB 查询/外部 API/内存变换/DB 写入/文件 IO/缓存）、输入输出类型、依赖节点、关联业务步骤。

## 存储
写入 `/documents/data-flow.md`，包含链路图（ASCII）、节点详情、输入输出映射。
